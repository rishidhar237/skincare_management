<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- Splash Activity (First Activity) -->
        <activity
            android:name=".SplashActivity"
            android:label="Splash"
            android:theme="@style/Theme.MyApplication"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:label="Login"
            android:exported="true" />

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:label="Signup"
            android:exported="true" />

        <!-- Main Activity (Home Page) -->
        <activity
            android:name=".MainActivity"
            android:label="Home"
            android:exported="true" />

        <!-- Edit Profile Activity -->
        <activity
            android:name=".EditProfileActivity"
            android:label="Edit Profile"
            android:exported="true" />

        <!-- Inventory Management Activity -->
        <activity
            android:name=".InventoryManagementActivity"
            android:label="Inventory Management"
            android:exported="true" />

        <!-- Product Info Activity -->
        <activity
            android:name=".ProductInfoActivity"
            android:label="Product Info"
            android:exported="true" />

        <!-- Product Detail Activities -->
        <activity
            android:name=".ProductDetailActivity1"
            android:label="Product Detail 1"
            android:exported="true" />

        <activity
            android:name=".ProductDetailActivity2"
            android:label="Product Detail 2"
            android:exported="true" />

        <activity
            android:name=".ProductDetailActivity3"
            android:label="Product Detail 3"
            android:exported="true" />
    </application>

</manifest>
